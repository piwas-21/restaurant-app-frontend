node_modules
.next
Dockerfile
.dockerignore
.git

# RUMI's own branding SOURCE set — build-image.yml's prod job copies it into
# public/branding/ in the runner, BEFORE the docker build, so RUMI keeps its assets
# while the repo default stays the SofraPiwas platform set. Excluded from the image
# so no tenant image carries another tenant's art.
public/branding-rumi
