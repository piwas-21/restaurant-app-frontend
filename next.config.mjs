/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // allowedDevOrigins: ["https://3001-ididl87degpltogqti9ee-5f8e2608.manus.computer"], // Temporarily comment out to avoid warning, will re-evaluate if needed for deployment
  },
  // Other configurations can go here
};

export default nextConfig;

