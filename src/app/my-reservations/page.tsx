// /my-reservations renders the active template's MyReservationsPage (ADR-006
// — my-reservations surface). Classic = the original list relocated to
// templates/classic/; craft = the docket-card stack re-skin (Prompt 7c).
// Both orchestrate the shared MyReservationsLayout.
export { default } from '@active-template/MyReservationsPage';
