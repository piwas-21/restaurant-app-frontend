// /reservations renders the active template's ReservationsPage (ADR-006 —
// reservations surface). Classic = the original page relocated verbatim to
// templates/classic/; craft = the hand-drawn seating-chart + booking-docket
// re-skin. Both orchestrate the shared ReservationsPageLayout.
export { default } from '@active-template/ReservationsPage';
